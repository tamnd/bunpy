[project]
name = "{{.Name}}"
version = "0.1.0"
description = "{{.Description}}"
authors = [{name = "{{.Author}}"}]
requires-python = "{{.PythonMin}}"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
